import 'package:flutter/material.dart';

class BNButton extends StatelessWidget {
  const BNButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
